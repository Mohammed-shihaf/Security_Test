# Application configuration (clean tree)

This directory intentionally contains **no** credentials. Use environment variables and your secret manager in real deployments.

Example (safe) placeholder:

```text
API_BASE_URL=https://api.example.com
```
